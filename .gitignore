*.iml
.gradle
/local.properties
.idea
.kotlin
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.
# Generated API reference (./gradlew dokkaToDocs) — built in CI, never committed
/docs/api/

# The built docs site (mkdocs build). CI deploys through mike, which builds its own copy.
/site/

# The local Python environment for the docs site. CI installs from docs-requirements.txt.
/.venv/

