<?xml version="1.0" encoding="utf-8"?><!--
    Firebase collection switches, kept out of the shared manifest so the FOSS build declares
    nothing Firebase-related at all. Both values come from the build type: true only in release.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsEnabled}" />
        <meta-data
            android:name="firebase_performance_collection_enabled"
            android:value="${performanceEnabled}" />

    </application>

</manifest>
